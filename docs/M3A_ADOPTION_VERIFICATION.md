# M3A adoption verification

Generated by `scripts/m3a_adoption_verify.py`. Package root `C:/Users/Swastik/Desktop/CRAG`,
opened read-only. Verify is not rebuild: nothing was written to the package and
no second relation vocabulary was produced.

| section | status |
|---|---|
| B -- MetaQA losslessness | **LOCALLY_REPRODUCED** |
| C -- WebQSP typed substrate | **LOCALLY_REPRODUCED** |
| C -- RoG-exact property | **UPSTREAM_ASSERTED_NOT_LOCALLY_REPRODUCIBLE** |

## B. MetaQA, recomputed from kb.txt

Source sha256 agrees with the
value the package recorded for it, so the file recomputed here is the file the
package built from.

| step | rows |
|---|---|
| source lines | 134,741 |
| blank lines | 0 |
| malformed lines | 0 |
| parsed triples | 134,741 |
| exact duplicates | 1,159 |
| distinct triples | 133,582 |
| mapped triples | 133,582 |
| unmapped triples | 0 |
| shipped graph rows | 133,582 |
| shipped graph malformed | 0 |
| shipped distinct edges | 133,582 |

The identity closes in both directions:

- `source = parsed + blank + malformed` — **True**
- `distinct = mapped + unmapped` — **True**
- local reconstruction equals the shipped graph — **True**

Unmapped rows are enumerated rather than counted; there are
0 of them. The upstream claims of 0 malformed and 0
dangling are independently established here, not accepted.

The locally reconstructed graph is equal to the transferred one. It is discarded. Equivalence is the result, not a licence to substitute.

| relation | edges |
|---|---|
| `starred_actors` | 33,718 |
| `has_tags` | 28,719 |
| `written_by` | 19,127 |
| `release_year` | 16,715 |
| `directed_by` | 15,957 |
| `has_genre` | 15,485 |
| `in_language` | 3,428 |
| `has_imdb_rating` | 314 |
| `has_imdb_votes` | 119 |

## C. WebQSP, against its own contract

7,058 relations, 8,309,195 edges,
2,592,894 nodes, counted from the shipped v1 tables.

| check | value |
|---|---|
| relation uid unique | True |
| relation key unique | True |
| relation short label collisions | 893 |
| relation qualified label collisions | 0 |
| null rows | 0 |
| dangling src | 0 |
| dangling dst | 0 |
| self loops | 93692 |
| duplicate edges | 0 |
| edges with unknown relation | 0 |

**The RoG-exact property is `UPSTREAM_ASSERTED_NOT_LOCALLY_REPRODUCIBLE`.**
Counting the shipped v1 tables confirms they agree with the recorded targets, which is internal consistency. It does not independently re-derive the union from the raw RoG shards, so the RoG-exact property itself remains upstream-asserted. The raw shards are present and the upstream rebuild took 1111.8s, so this is affordable if review wants it.

That status is not a rejection; it goes to review.

**Two things the shipped tables make visible that the transfer manifest did not.**

The graph is the WebQSP ∪ CWQ union. The 8,309,195-triple graph is the union. WebQSP alone contributed 3,791,303 distinct triples. Evaluating WebQSP queries against the union gives a candidate universe containing CWQ-only triples, which is a different graph from 'WebQSP's graph' and has to be a declared choice.

The endpoint count carries its own caveat from source:
*Endpoints are REPORTED, never an acceptance criterion. The paper's 2,566,291 and the prior audit's measured 2,592,894 differ by +1.04% and no counting convention explains it (strip / whitespace / NFC / casefold / drop-literals / head-only all tested and rejected).*

And the encode has a choice to make: The encode has to pick a text field, and the choice changes what the semantic features can express and what the encode costs. It is a scientific choice, not a systems one, and belongs to review.

## F. The two relation regimes

| dataset | types | edges | entropy | max | normalised | top-1 | top-10 |
|---|---|---|---|---|---|---|---|
| metaqa | 9 | 133,582 | 2.6465 | 3.1699 | 0.8349 | 0.2524 | 1.0 |
| webqsp | 7,058 | 8,309,195 | 8.4282 | 12.785 | 0.6592 | 0.047 | 0.231 |

WebQSP's vocabulary is **784.2×** MetaQA's.
A MetaQA null must never be reported as 'typed relation features do not help'. MetaQA has 9 relation types against WebQSP's 7058; a null there is about that regime.

Relation text is **not** encoded. Not encoded yet. Section F requires the audit first, and this is the audit.

Machine-readable form: `outputs/m3a/adoption_verification.json`.
