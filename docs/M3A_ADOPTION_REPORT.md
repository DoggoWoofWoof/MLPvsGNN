# M3A adoption report

Generated by `scripts/m3a_adoption_report.py` from the item 1-7 artifacts, which it reads
rather than restates. This document decides nothing; it recommends, and the contract ends at STOP_FOR_REVIEW.

| section | | verdict |
|---|---|---|
| A | transfer ingest gate | **ADOPT** |
| B | MetaQA local losslessness | **ADOPT** |
| C | WebQSP typed substrate | **ADOPT_WITH_FINDING** |
| D | historical substrates untouched | **ADOPT** |
| E | node-space alignment | **ADOPT_EXCEPT_WEBQSP** |
| F | relation representation audit | **ADOPT_PENDING_REVIEW** |

## Overall: ADOPT_WITH_FINDINGS

Every section that could be closed by verification alone is closed. Nothing failed. No section requires repair, so the 'verification fails and a later review authorises repair' branch of decision 2 is not triggered. What remains open is not defect but scope: four decisions this phase is not authorised to take.

Verify-is-not-rebuild held throughout: **0** bytes of the
package modified, **no** second relation vocabulary, **no** second graph. Every
artifact this phase produced is a sidecar.

## What each section found

### A. transfer ingest gate

**ADOPT** — 2,841 files, 81.618 GiB, inventoried read-only and hashed. The manifest is write-once; a second pass re-hashed and diffed rather than overwriting. Nothing in the package was modified.

*Source: `outputs/m3a/transfer_manifest.json`*

### B. MetaQA local losslessness

**ADOPT** — 134,741 source lines recomputed from kb.txt to 133,582 distinct triples to 133,582 mapped edges, 0 unmapped and enumerated. The accounting identity closes in both directions (source = parsed + blank + malformed: True; distinct = mapped + unmapped: True), and the locally reconstructed graph is set-equal to the shipped one. Upstream's claims of zero malformed and zero dangling are independently established here, not accepted. The local reconstruction was discarded: equivalence is the result, not a licence to substitute.

*Source: `outputs/m3a/adoption_verification.json#section_b`*

### C. WebQSP typed substrate

**ADOPT_WITH_FINDING** — 7,058 relations, 8,309,195 edges, 2,592,894 nodes, counted from the shipped v1 tables against the source contract. No second vocabulary was built. Three things the upstream manifest does not record: 893 relation SHORT-label collisions against 0 qualified-label collisions, so relation text must come from relation_label_qualified or 893 distinct relations silently merge; 93,692 self-loops; and the graph is the WebQSP union CWQ, of which WebQSP alone contributed 3,791,303 distinct triples against CWQ's 7,269,449. The RoG-exact property is UPSTREAM_ASSERTED_NOT_LOCALLY_REPRODUCIBLE: counting the shipped tables shows internal consistency, not a re-derivation from the raw shards. The shards are present and the upstream rebuild took 1,111.8s, so re-deriving is affordable if review wants it.

*Source: `outputs/m3a/adoption_verification.json#section_c`*

### D. historical substrates untouched

**ADOPT** — No historical artifact was read for content, modified, re-hashed or compared against a canonical number. The clean development substrate, the old graph.pt and the frozen M2 results are as they were. No table in this phase places a canonical figure beside a historical one.

*Source: `this phase produced no cross-substrate arithmetic`*

### E. node-space alignment

**ADOPT_EXCEPT_WEBQSP** — The chain graph node -> canonical id -> embedding row -> retrieval row is one-to-one and green on 5 of 6 datasets (2wiki, hotpotqa, metaqa, musique, squad), reported in both directions. webqsp is blocked for the one reason item 6 exists. Measured and not previously recorded: canonical nodes with no edge at all -- squad 6,349 (31.4%), musique 9,858 (8.4%), hotpotqa 551,848 (10.5%), 2wiki 515,830 (8.6%); metaqa, webqsp have none. Those nodes bound what any structural feature can say, and the model must see the mask rather than the zero.

*Source: `outputs/m3a/node_alignment.json`*

### F. relation representation audit

**ADOPT_PENDING_REVIEW** — Two regimes, not one. MetaQA 9 relation types at normalised entropy 0.8349, median 15,957 edges per relation and no singletons; WebQSP 7,058 types at 0.6592, median 20 edges per relation and 902 singletons -- a 784.2x vocabulary with a long tail that a one-hot relation feature would mostly waste. Relation text is NOT encoded, which is what section F requires: the audit comes first and this is the audit. The consequence is a reporting rule -- a MetaQA null is about a 9-relation regime and may never be generalised to 'typed relation features do not help'.

*Source: `outputs/m3a/adoption_verification.json#section_f`*

## What review has to decide

Four decisions are open. None of them is a defect; each is something this phase
was not authorised to settle.

### 1. WebQSP node text representation

name_plus_facts folds incident triples into node text, moving relational information into the semantic channel. QLS-U would then receive graph structure through its embeddings regardless of which structural features are declared, and delta_MP would no longer isolate message passing. It is also the option that cannot run: its length-sorted peak batch is 84.0 GiB of activations against a 24 GiB card, deterministically on the first batch.

**Recommendation:** name_only, but this is review's call, not this phase's

### 2. whether to build a canonical WebQSP query set

WebQSP v1 ships no queries at all -- no queries/ directory, unlike every other dataset. So 'number of missing query embeddings' has no answer as an encode quantity. The raw material exists, but constructing splits is a BUILD, and decision 2 says verify is not rebuild. Without it WebQSP cannot be evaluated, so decision 1's 'WebQSP is a priority dataset' cannot be honoured without authorising this.

**Recommendation:** none offered; this is squarely a review decision

### 3. candidate universe for WebQSP queries

The shipped graph is WebQSP union CWQ. Evaluating WebQSP queries against the union means the candidate universe contains CWQ-only triples. That is a different graph from 'WebQSP's graph' and has to be declared either way, not defaulted into.

**Recommendation:** declare explicitly whichever way it goes

### 4. whether to re-derive the RoG-exact property from raw shards

It is currently upstream-asserted. Re-deriving costs about 1,112s of CPU by upstream's own timing, which is cheap, but it is a rebuild and this phase is not authorised to perform one.

**Recommendation:** affordable if review wants the property locally established

## WebQSP encode readiness

The node encode is priced and ready: 1.08-3.24 dense, 0.18-0.51 splade (name_only), about
$1.39-4.12, against a hard ceiling of
16.2 dense GPU hours. It is blocked on
the representation choice and the query-set decision.

The query encode **cannot** be priced: there is no query set to price. Decision 1
made WebQSP a priority dataset rather than an optional appendix, and that
commitment cannot be met by encoding alone.

## Not done, and not authorised

- construct final QLS-U features
- construct high-headroom candidates
- train QLS-U
- train GAT
- train GAT-NO-MP
- run the WebQSP encode itself
- build a WebQSP query set
- recompute headroom (section I: after adoption and after the encode)

## STOP_FOR_REVIEW

Machine-readable form: `outputs/m3a/adoption_report.json`.
