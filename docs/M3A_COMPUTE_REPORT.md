# M3A compute and storage, measured

Generated by `scripts/m3a_compute_report.py`. This document decides nothing; it records what was spent and tests section J's claim.

## What this phase spent

| | |
|---|---|
| GPU seconds | **0.0** |
| Wall seconds | 442.0 (7.4 minutes) |
| Cost | **$0.0** |
| Package bytes modified | **0** |

Zero GPU seconds. Every item in the current authorisation was CPU and disk: hashing, counting, set arithmetic and parquet scans. No model was loaded, no fit was run and nothing was encoded.

Read 81.618 GiB across 2,841 files at
430.1 MiB/s. Wrote 0.82 MiB of sidecar
artifacts — a read-to-write ratio of about **102,008:1**,
which is the shape a verification phase should have. Everything written is a sidecar in this repository. outputs/ is gitignored repo-wide, so the artifacts are regenerated by their scripts rather than committed; the docs are committed.

| item | | wall s | GPU s | KiB written |
|---|---|---|---|---|
| 1 | transfer ingest gate | 194.3 | 0.0 | 787 |
| 2/3/4/7 | adoption verification | 38.0 | 0.0 | 12 |
| 5 | node-space alignment | 202.7 | 0.0 | 11 |
| 6 | WebQSP encode budget | 7.0 | 0.0 | 14 |
| 8 | adoption report | 0.0 | 0.0 | 15 |

## Section J's claim, tested

> verification should be substantially cheaper than reconstruction


**MetaQA typed graph** — NOT_A_TEST_OF_THE_CLAIM. folded into the adoption verification run; section B does perform a full local reconstruction, so for MetaQA verification and reconstruction cost the same and the section J claim is not tested here -- it is 133,582 edges, cheap either way.

**WebQSP typed substrate (8,309,195 edges, 7,058 relations)** — verified in 38.0s (counting and checking the shipped v1 tables against their own contract); reconstruction took 1111.8s (upstream's own recorded elapsed_s for the RoG union rebuild from 29 raw parquet shards totalling 4.02 GB -- ROG_UNION_REBUILD.json). Verification is **29.3x cheaper**. The claim holds.

## Projected, not spent

The WebQSP node encode would cost 1.08-3.24 dense, 0.18-0.51 splade, about
$1.39-4.12, producing
7.4 GiB
(name_only; see item 6). Status
**NOT_AUTHORISED_TO_RUN**.

The query encode is **CANNOT_BE_PRICED**:
there is no canonical query set to price.

Re-deriving the RoG-exact property locally would cost about
1111.8s of CPU
(upstream's own recorded timing). Status
**OFFERED_TO_REVIEW_NOT_PERFORMED**.

## Re-runs, and why

**transfer inventory, re-hashed with --reinventory.** the WebQSP identity block read keys that do not exist in ROG_UNION_REBUILD.json, so the inventory printed '?' where the relation and triple counts belong. The re-run both fixed the derived field and re-established that the package bytes had not moved: 0 added, 0 removed, 0 changed.

**adoption verification and node alignment, once each.** neither recorded a wall time on its first run, and section J asks for a measured compute report. A wall time that was never written down cannot be recovered from the artifact afterwards, so the scripts were instrumented and run once. Their outputs are deterministic and unchanged; only the measured_cost block is new.

Nothing was re-run merely to fill a row that already held the information. The manifest's hashing figure is the one it recorded on its own pass, and the inventory doc was redrawn from the manifest on disk rather than by re-hashing 81.6 GiB to change one table cell.

Machine-readable form: `outputs/m3a/compute_report.json`.
